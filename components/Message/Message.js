import {
    Container,
} from "./styledMessage";

const Message = ({ user, message }) => {
    return (
        <Container>
            <h1>Message Screen</h1>
        </Container>
    );
};

export default Message;
